// Definir las variables
let a = 8;
let b = 5;

// Calcular ga según la fórmula dada
let ga = 2 * b + Math.floor(a / 2) + 4 * (b % a);

// Mostrar el resultado
console.log("El resultado es:", ga);
